<UserControl x:Class="autoplaysharp.App.UI.Tasks.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:tasks="clr-namespace:autoplaysharp.App.UI.Tasks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Path=TasksViewModel, Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock>Active:</TextBlock>
                <TextBlock Text="{Binding Active.Name}"/>
                <Button Command="{Binding Active.Cancel}">Cancel</Button>
            </StackPanel>
            <ListView ItemsSource="{Binding Queue}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}">
                            <TextBlock.ContextMenu>
                                <ContextMenu>
                                    <MenuItem HorizontalContentAlignment="Stretch" Header="Cancel" Command="{Binding Cancel}"/>
                                </ContextMenu>
                            </TextBlock.ContextMenu>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <ComboBox ItemsSource="{Binding Tasks}" DisplayMemberPath="Name" x:Name="_tasksComboBox"/>
            <ContentControl Content="{Binding Path=SelectedItem, ElementName=_tasksComboBox}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type tasks:AllianceBattleViewModel}">
                        <StackPanel>
                            <CheckBox Content="Run Normal Mode?" IsChecked="{Binding RunNormalMode}"/>
                            <CheckBox Content="Run Extreme Mode?" IsChecked="{Binding RunExtremeMode}"/>
                        </StackPanel>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
            <Button Command="{Binding Path=SelectedItem.AddToQueue, ElementName=_tasksComboBox}">Add</Button>
        </StackPanel>
    </Grid>
</UserControl>
