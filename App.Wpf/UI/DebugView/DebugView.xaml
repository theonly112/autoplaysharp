<UserControl x:Class="autoplaysharp.App.UI.DebugView.DebugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Path=DebugViewModel, Source={StaticResource Locator}}">
    <Grid>
        <StackPanel>
            <Label>TaskName</Label>
            <TextBox Text="{Binding TaskName}"/>
            <Button Command="{Binding Run}">Run</Button>
            <CheckBox Content="Enable Overlay? (restart to apply)" IsChecked="{Binding EnableOverlay}"/>
            <Button Command="{Binding Drag}">Drag</Button>
            <CheckBox Content="Restart on error" IsChecked="{Binding RestartOnError}"/>
            <CheckBox Content="Enable recording" IsChecked="{Binding EnableRecording}"/>
            <TextBox Text="{Binding FrameRate}"/>
            <Button Command="{Binding StartRecording}">Start recording</Button>
            <Button Command="{Binding EndRecording}">End recording</Button>
            <TextBox Text="{Binding RecordingDirectory}"/>

        </StackPanel>
    </Grid>
</UserControl>
